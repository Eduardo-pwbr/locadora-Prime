
## 🔐 Configuração da API Key (.env)

Este projeto utiliza uma API externa que requer autenticação através de uma chave (API Key).

Você pode criar sua chave acessando o site da API:

👉 https://brasilprimeflix.netlify.app
(Crie uma conta e gere sua API Key)

### 🚀 Como configurar

1️⃣ Na raiz do projeto, crie um arquivo chamado `.env`

2️⃣ Dentro dele, adicione a seguinte variável:

```env
REACT_APP_API_KEY=sua_chave_aqui

